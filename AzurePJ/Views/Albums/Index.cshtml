@model List<AzurePJ.Models.AlbumViewModel>

<div class="album-header d-flex align-items-center justify-content-between mb-3">
    <h2 class="album-title m-0">♡アルバム♡</h2>
    <button class="cool-btn album-create-btn" onclick="location.href='/Albums/Create'">アルバムを作成</button>

</div>

<ul class="album-list">
    @foreach (var album in Model)
    {
        <li class="album-item">
            <a class="thumbnail-link" href="/Albums/Folder/@album.Id">
                <div class="thumbnail-img-container">
                    @if (!string.IsNullOrEmpty(album.CoverUrl))
                    {
                        <img src="@album.CoverUrl" alt="@album.Name" />
                    }
                    else
                    {
                        <span class="thumbnail-placeholder">@album.Name</span>
                    }
                </div>
                <div class="album-name">@album.Name</div>
            </a>

            <form asp-action="Delete" method="post" class="album-delete-form">
                <input type="hidden" name="id" value="@album.Id" />
                <button type="submit" class="album-delete-btn"
                        title="アルバムを削除"
                        onclick="return confirm('本当に削除しますか？');">
                    🗑️
                </button>
            </form>
        </li>
    }
</ul>
